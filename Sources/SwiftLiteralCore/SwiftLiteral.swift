// Re-exports, so `import SwiftLiteralCore` is enough to use the engine's types.
//
// This file used to also carry a hand-maintained index of every public type and where it
// lived. Two of the entries named things that had been deleted. An index nobody updates is
// worse than no index; the module page in DocC is generated from the real symbols.

@_exported import Foundation
@_exported import SwiftLiteralReflection
